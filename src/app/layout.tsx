import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Squeaky Spring Cleaning - Professional Cleaning Services in Orlando',
  description: 'Professional eco-friendly cleaning services in Orlando. Move in/out cleaning, office cleaning, childcare facilities, and Airbnb cleaning services.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}